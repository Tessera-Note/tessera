# Что осталось делать

Только открытая работа. Сделанное сюда не возвращается: его записи лежат в
`done/`, включая оба закрытых чек-листа — `done/25-checklist-groups.md`
(перенос, четырнадцать групп) и `done/26-checklist-parity.md` (совпадение с v1,
шесть разделов).

Состояние снято по коду 4 сентября 2026.

## Как ведётся этот документ

Пункт живёт здесь, пока не сделан. Сделанный **удаляется отсюда**, а его
подробности уходят отдельным файлом в `done/`: что было, что сделано, чем
проверено. Галочек `[x]` в этом документе не бывает — сделанного здесь нет
вовсе.

Пункт, который решено не делать, тоже удаляется, а причина уходит в
`../future-roadmap.md`.

## Куда идёт работа

**Боевой домен не трогаем.** Переключение живой вики отложено, план на него
написан и отрепетирован (`09-switchover.md`), возвращаться к нему по команде.

**Цель — самостоятельное развёртывание на этой машине, на `localhost`.**
Достигнута: состав поднимается целиком, пять действий из `09-switchover.md`
пройдены в браузере (`done/27-local-deployment.md`). Всё, что ниже, оценивается
тем же — работает ли оно на поднятом развёртывании, без выхода в интернет и без
боевого домена.

## 1. Экраны v2 против экранов v1, поимённо

Открыто владельцем со снимками обоих экранов разговора: «в версии 1 всё красиво
и правильно, а в версии 2». Повод важнее самого экрана. До этого совпадение с
v1 понималось как совпадение поведения, и в чек-листах стояло «совпадение с
v1» там, где совпадал только набор действий: экран делал то же самое и выглядел
при этом заготовкой.

Порядок работы задан владельцем: пройти все экраны со снимками и составить
список расхождений — не «стили некорректные», а поимённо, что на экране v1 есть
и чего у v2 нет; начать с разговора; дальше по частоте использования —
страница, дерево, поиск, настройки.

Разговор закрыт (`done/33-chat-screen.md`). Дерево закрыто в той части, где
ветвь уходила за край панели и названия сжимались в ничто.

Осталось пройти: **страница**, **поиск**, **настройки**, затем остальные экраны
списка. По каждому — снимок v1, снимок v2, поимённый список недостающего.

Отдельно замечено на ввезённой странице и пока не разобрано: заголовок виден
дважды — один раз заголовком страницы, второй раз первой строкой содержимого.
Так лежит в выгрузке; как с этим поступает v1, не сверено.

## 2. Ввоз PDF: разбор уходит в `services/collab`

v1 читает PDF пакетом `@docmost/pdf-inspector` и получает Markdown с
заголовками и списками, v2 — `pypdf` и получает голый текст.

**Решение: тем же пакетом, в сервисе преобразования содержимого.** Не своим
разбором расположения текста и не новой службой в составе.

Основания. Пакет уже лежит в файле блокировки — его объявляет `apps/server`,
то есть новой зависимости в развёртывании не появляется вовсе, а после удаления
`apps/server` объявление просто переезжает. Сервис `services/collab` для этого и
существует: там уже живут преобразования HTML, Markdown и вывоз DOCX, и это
закрытое исключение проекта на Node. Пакет тот же, что у v1, значит и точность
разбора та же — сличать нечего.

Свой разбор расположения текста означал бы третью реализацию того, что уже
дважды написано, и качество его пришлось бы замерять с нуля. Отдельная служба в
составе — лишний контейнер ради одной функции, которой есть место в имеющемся.

## Что в этот список не входит

**Переключение боевого домена.** Отложено решением владельца, см. выше.

**Правки в ветке `main`.** Пять дефектов v1 описаны в `../future-roadmap.md`;
v2 ни один из них не унаследовала, проверено по коду. Ветка `main` — прод и
живёт как есть, туда без отдельной команды не ходим.

**Рассылка совместного редактирования между репликами.** Нужна, только если
экземпляров сервиса станет больше одного. Обоснование в `done/12-deferred.md`.

**Вычитка переводов носителями языка.** Все двенадцать словарей заведены
полностью, тексты писем тоже, но переводы сделаны в работе. Это не
программирование.

**Ограничитель частоты на маршрутах SCIM.** Замерено, а не отброшено: токен —
256 бит из `secrets.token_urlsafe`, хранится хешем, сравнивается за постоянное
время (`hmac.compare_digest`). Перебор по сети такому не угрожает, и предел
частоты не добавил бы ничего. Четыре открытых маршрута отдают статическое
описание протокола. В v1 ограничителя здесь тоже нет.
