# 13. Документ отложенных задач

## Было

`docs/future-roadmap.md` числил открытыми пункты, закрытые давно. Опись остатка
называла пять; сверка нашла восемь.

## Сделано

Убрано восемь разделов. Каждый — со сверкой по коду, а не по самой записи:
запись про отсутствие проверяется чтением кода, иначе она переписывает саму
себя.

### Закрыто работой этого захода

| Раздел | Чем закрыт |
|---|---|
| Словари i18next: остались девять локалей | `done/09-locales.md`; во всех двенадцати словарях по 2257 ключей |
| Коды отказов и новые строки заведены в трёх словарях из двенадцати | там же |
| Коды отказов v2 в словарях не заведены | `done/08-error-texts.md`; все 218 кодов заведены, проверка ловит код без подписи |
| Подписку на страницу нельзя отключить из интерфейса | `done/03-page-actions.md`; кнопка подписки на странице |

### Числилось открытым, а закрыто раньше

| Раздел | Чем опровергнуто |
|---|---|
| Настройки уведомлений в v2 не читаются | `services/notifications.py:377-383` — отбор получателей читает `users.settings.notifications` и отсеивает по признаку |
| Групп в v2 нет, кроме синхронизируемых каталогом | `api/spaces.py:491` — `GroupController` с созданием, правкой, удалением и составом; экран `/settings/groups` с `[groupId]` |
| Список сессий и их отзыв | `api/auth.py:250-299` — `/sessions`, `/sessions/revoke`, `/sessions/revoke-all`; на клиенте `features/auth/services/sessions.ts` |
| Сервер разработки фронта v2 падает по памяти | измерено: `vite dev` проработал 5 ч 58 мин под нагрузкой этой сессии, RSS 1,64 ГБ, `/login` отвечает 200. Запись описывала падение через полминуты |

### Уточнено, а не убрано

Раздел про словари заменён: отставание девяти локалей закрыто, но открытым
остаётся, чем вести их дальше. `crowdin.yml` в репозитории есть, а русский,
украинский и теперь девять остальных правились напрямую — при запуске
синхронизации эти переводы надо загрузить в Crowdin как переводы, иначе
следующая выгрузка их затрёт. Та же оговорка была в прежней записи и осталась
верной.

Из раздела «Наблюдения этого захода» убрана строка про отставание девяти локалей
на 640 ключей: она перестала быть верной.

## Проверено

- Каждое из восьми удалений подтверждено чтением кода или измерением;
  результаты сведены в таблицы выше.
- Разделов в документе осталось четырнадцать, все проверены на месте.
