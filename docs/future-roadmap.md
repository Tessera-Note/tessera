# Отложенные доработки

Единственный документ для отложенных задач — и функциональных, и
инфраструктурных. Здесь только то, что ещё предстоит сделать: реализованный
пункт из документа удаляется в той же сессии, в которой закрыт.

Состояние сверено с кодом 14 сентября 2026.

Записи о закрытом и разбор первой версии убраны отсюда: они остаются в истории
репозитория (`git log -- docs/future-roadmap.md`). Первая версия живёт
ретро-веткой `v1` и тегом `v1-final`, новых правок там не будет, и её
недоработки в этот перечень не входят.

## Вторая реплика службы редактирования

Совместное редактирование вынесено отдельным сервисом `services/collab`, и в
`apps/api/docker-compose.v2.yml` он поднимается ровно в одном экземпляре.
Состояние открытых документов живёт в памяти процесса: со второй репликой
правки двух человек на разных репликах не сойдутся — молча, без единого отказа.
Запрет поднимать второй экземпляр записан там, где его увидит разворачивающий
(`docs/v2-migration/09-switchover.md` и комментарий в самом compose-файле).

Прежнее решение — закрепление соединений в прокси по имени документа
(`hash $arg_documentName consistent;`) — **опирается на неверную посылку**.
Проверено по коду библиотеки 14 сентября 2026: `HocuspocusProviderWebsocket`
открывает соединение по голому адресу (`ws://узел/collab`), а имя документа
уходит первым сообщением протокола (`writeVarString(documentName)`). В адресе
его нет, и `$arg_documentName` в nginx всегда пуст — правило закрепило бы все
соединения на одну реплику.

Что можно сделать.

1. **Ничего, пока экземпляр один.** Нагрузка на процесс не упирается в счёт:
   десять одновременных соединений на страницу дают 107 МБ памяти и 0,1 % ЦП
   (замер 14 сентября 2026, `scripts/collab-load.mjs`). Давит не ёмкость, а
   доступность: перезапуск рвёт все соединения разом. Набранное при этом не
   теряется — оно лежит в хранилище браузера и доезжает после переподключения.
2. **Вписать имя документа в адрес подключения и закрепить соединения
   прокси.** У клиента это один довод (`url: .../collab/page.<id>` либо
   `?documentName=page.<id>`), протокол не меняется — сервер по-прежнему
   читает имя из сообщения, адрес нужен только прокси. Каждое соединение
   держит свой сокет (провайдер заводит его сам, когда готовый не передан),
   поэтому одно соединение — один документ, и закрепление однозначно.
   Рекомендуемый путь: правило в прокси и одна строка на клиенте.
3. **Перенести рассылку состояния через Redis**, как в v1
   (`redis-sync`, 378 строк). Отвергнуто: эти строки пришлось бы переписать на
   Node и держать в согласии с правами на Python.

## Смена идентификатора и почты у провайдера разом

Вход через провайдера ищет человека по идентификатору и по почте. Если у
провайдера сменилось и то и другое, ни один поиск не находит, и при включённой
регистрации заводится вторая учётная запись. Данных, по которым это отличить от
первого входа нового сотрудника, у приложения нет.

Что можно сделать.

1. **Второй признак сопоставления.** У провайдера завести поле «какое
   утверждение считать неизменным ключом» (`employeeNumber`,
   `preferred_username` — что есть в схеме конкретного провайдера), хранить его
   значение рядом со связью. Тогда смена идентификатора и почты разом всё равно
   находит человека. Это настоящее решение, но оно работает только там, где
   провайдер такое утверждение отдаёт.
2. **Страховка на случай, когда утверждения нет: не заводить вторую запись
   молча.** Событие журнала `user.sso_possible_duplicate` при заведении записи,
   чьё имя совпадает с именем действующего участника, и действие
   распорядителя «это тот же человек»: связь перевешивается на прежнюю запись,
   новая отключается. Дешевле первого пункта и закрывает именно молчаливость.
3. **Выключить самостоятельную регистрацию через провайдера.** Ненайденный
   человек получает отказ, распорядитель заводит связь руками. Надёжно, но
   ломает приём новых сотрудников.

Рекомендация: (2) как обязательное, (1) там, где провайдер это позволяет.

## Отказы SCIM остаются английскими

Двенадцать сообщений в SCIM переведены не будут, и это решение, а не
недоработка: ответ уходит провайдеру учётных записей в форме, заданной
RFC 7644, разбирает его программа, а собственный фильтр протокола строит тело
сам, без поля кода. Кода и перевода там не будет.

Что при этом стоит сделать: поле `detail` в интерфейсе провайдера (Okta, Entra)
видит его администратор, и язык его нам неизвестен — заголовка с языком в
запросах SCIM провайдеры не шлют. Поэтому не перевод, а точность: каждое из
двенадцати сообщений должно называть поле и значение, на котором споткнулась
синхронизация, чтобы администратор понял причину без перевода. Проверить все
двенадцать и переписать те, что этого не делают.

## Словари не вычитаны носителями языка

Во всех двенадцати словарях по 2372 ключа, расхождений нет. Переводы сделаны в
работе и носителями языка не вычитаны — это надо знать, принимая результат.

**Про Crowdin.** Это сторонняя служба управления переводами: исходные строки
выгружаются в её веб-интерфейс, там с ними работают переводчики, готовое
забирается обратно. Соответствие файлов описано в `crowdin.yml`.

Выключен намеренно, 6 сентября 2026. Переводчиков со стороны нет, экземпляр
закрытый, а нынешние словари заведены переводами, сделанными в работе, — в
Crowdin их нет, и первая же выгрузка вернула бы двенадцать языков в английский
молча. `crowdin.yml` приведён в нерабочее состояние: записи закомментированы,
`files` пуст, и команда `crowdin` на таком составе отказывается работать. Там
же записан порядок включения, если решение изменится: сперва загрузить нынешние
словари как переводы, убедиться выгрузкой в отдельную ветку, что ни один ключ
не вернулся английским, и только потом снимать комментарии.

Включать не надо, пока не появились переводчики со стороны. Для вычитки
хватает своих средств.

Что можно сделать.

1. **Выгрузка разницы на вычитку.** Скрипт, который по двум точкам истории
   собирает ключи, добавленные или изменённые с прошлой вычитки, и кладёт их
   таблицей на язык; обратный скрипт вносит правки в словарь. Вычитывающий
   работает с двумя десятками строк, а не с двумя тысячами, и ничего, кроме
   файла, наружу не уходит.
2. **Пометка вычитанного.** Отдельный файл со списком языков и точкой истории,
   до которой словарь прочитан носителем. Без него «вычитано» остаётся устным
   знанием и теряется.
3. **Crowdin по порядку из `crowdin.yml`** — только если появятся переводчики
   со стороны.

## Выгрузка PDF обрывается на сотне страниц молча

`services/pdf_export.py`, `_viewable_branch`: `ids[:MAX_PAGES]`, `MAX_PAGES =
100`. Ветвь из полутораста страниц выгружается сотней, и человек считает
полученный файл полным. След остаётся только в журнале сервера.

Чинится одним из двух: назвать частичность в самой выдаче (число страниц в
задании и отметка «выгружено 100 из 150», видная на экране переноса) либо
отказать явно и предложить выгружать по частям. Молча продолжать нельзя —
это тот же класс, что описан в `docs/ai-context/silent-divergence.md`.

## Встроенный ролик в PDF — пустая рамка

Проверено глазами 14 сентября 2026 на выгрузке страницы «Все виды содержимого».
Узел `embed` рисуется `<iframe>`, а в печати он не загружается — ни на закрытом
контуре, ни через список разрешённых адресов Gotenberg, — и на листе остаётся
пустой прямоугольник в половину страницы.

Чинится показом на печати вместо рамки: название и адрес ролика ссылкой. Читать
по бумаге всё равно нечего, а адрес переносит читателя туда, где ролик есть.

## Ввоз ODT не переносит картинки

`services/odt_import.py` разбирает разметку документа, но картинки оставляет:
в ODT они лежат в `Pictures/` и связаны через `draw:image`, а ввоз одиночного
файла кладёт вложения только для Word — там их отдаёт разборщик. Документ
приезжает с текстом, таблицами и списками, без картинок и без предупреждения.

Чинится так же, как у Word: разборщик отдаёт перечень картинок вместе с
разметкой, общий путь ввоза кладёт их вложениями и подставляет свои адреса.

## Кеш прав страницы: пересмотреть, когда таблицы наполнятся

Вопрос закрыт замером, а не рассуждением: `canUserEditPage` выполнялся за
0,154 мс при пустых `page_access` и `page_permissions`. Кеш защищал бы запрос
дешевле собственного обращения к Redis и покупал бы окно, в котором снятое
право продолжает действовать.

Условия изменились наполовину: маршруты правки прав появились, значит таблицы
перестанут быть пустыми. Пересматривать, когда в них накопятся реальные объёмы,
и замерять заново на настоящей глубине дерева, а не переносить прежнюю оценку.

Схема на случай, если ответ изменится: метка поколения, ключ вида
`perm:can-edit:g<метка>:<userId>:<pageId>`, любое изменение прав меняет метку.
Метка глобальная, а не на пространство: правки прав редки, а глобальную
невозможно забыть сбросить.

## Мелкое

- **`cross-env` в корневом манифесте не используется.** Объявлен в
  `package.json`, не встречается больше нигде. Проверять удаление надо сборкой,
  а не поиском по исходникам: `clsx` выглядел так же и оказался нужен самому
  Svelte.
- **Боевой домен остаётся в двух записях.** `docs/superpowers/plans/2026-07-31-emails-pt-br.md`
  и `docs/templates/documentacao-tecnica/PROMPT-PARA-IA.md`. Это запись о
  сделанном и шаблон, к развёртыванию отношения не имеют.

## Наблюдения, не дефекты

**Переменная `SEARXNG_SECRET` в compose ни на что не влияет.** Образ SearXNG
подставляет её правкой `/etc/searxng/settings.yml` на старте, а файл смонтирован
только на чтение, поэтому подстановка не проходит и остаётся литерал из
`deploy/searxng/settings.yml`. Приемлемо, пока сервис не публикуется наружу:
ключ подписывает состояние форм собственного интерфейса SearXNG, которым
приложение не пользуется. Если сервис откроют наружу, ключ надо выносить в том
с правом записи, а строку из compose либо задействовать, либо убрать.

**Перечень присутствующих держит ушедшую вкладку до истечения срока awareness.**
Видно после перезагрузки страницы: прежняя вкладка ещё несколько секунд стоит в
перечне отдельной записью. Проходит само, лечится только на стороне библиотеки.

## Что не входит в этот документ

Задачи текущей сессии ведутся средствами задач, а не здесь. Сюда попадает только
то, что отложено осознанно и требует отдельного решения.
