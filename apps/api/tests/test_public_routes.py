"""Опись маршрутов, доступных без аутентификации.

Правило из v1: `@Public` меняет поверхность аутентификации, и ставить его надо
осознанно. Там опись завели, когда публичных маршрутов накопилось тридцать
шесть, и добавление тридцать седьмого ничем не отличалось от обычной правки.
Здесь она заводится с первого дня.

Проверка не запрещает публичные маршруты. Она делает их появление видимым:
новый роняет её, и список правится тем же коммитом, где появился маршрут.
"""

from __future__ import annotations

from litestar import Litestar

from tessera_api.api.guards import PUBLIC
from tessera_api.app import create_app
from tessera_api.config import Settings

#: Маршруты, работающие без токена. Каждый обоснован здесь же.
EXPECTED_PUBLIC = {
    # Готовность проверяет оркестратор, у которого токена нет и быть не может.
    # Закрытая проверка означает, что контейнер вечно нездоров.
    "/api/health",
    # Вход: до него токена ещё нет.
    "/api/auth/login",
    # Настройка пустого экземпляра: до неё в базе нет никого, и требовать токен
    # значило бы требовать войти туда, куда войти нельзя. Второй раз маршрут не
    # срабатывает, настроенный экземпляр отвечает отказом.
    "/api/auth/setup",
    # Экран настройки спрашивает это до входа.
    "/api/auth/setup-required",
    # Принятие приглашения: приглашённого в базе ещё нет, и требовать токен
    # значило бы требовать войти до того, как учётная запись заведена.
    # Учётными данными служит токен приглашения.
    "/api/workspace/invites/accept",
    # Сброс пароля: человек не помнит пароля, войти он не может.
    "/api/auth/forgot-password",
    "/api/auth/password-reset",
    "/api/auth/verify-token",
    # Принудительная настройка второго фактора: тот же промежуточный шаг.
    # Без этих двух маршрутов требование пространства запирало бы человека на
    # экране входа — сессию ему не выдают, а настроить фактор нечем.
    "/api/mfa/enroll-setup",
    "/api/mfa/enroll-enable",
    # Завершение входа вторым фактором: обычной сессии на этом шаге ещё нет.
    # Учётными данными служат промежуточный токен из куки, выданный после
    # сверки пароля, и код из приложения. Предел частоты тот же, что у входа.
    "/api/mfa/challenge",
    # Ссылка общего доступа заводится ради тех, у кого учётной записи нет.
    # Учётными данными служит ключ ссылки.
    "/api/share/open",
    # Аватары и логотипы. Открыты по той же причине, что и в v1: они
    # показываются на страницах, открытых по ссылке общего доступа, где
    # вошедшего нет вовсе, и закрытый маршрут сломал бы публичные ссылки.
    #
    # Защита не в правах, а в имени файла: оно случайное, шестнадцать байт, и
    # не перечисляется ниоткуда. Содержимое при этом не секрет — аватар видит
    # каждый, кто видит человека в интерфейсе. Содержимое страниц этим
    # маршрутом не отдаётся: он ходит только в разделы аватаров и логотипов,
    # и вид вложения сверяется со списком.
    "/api/attachments/img/{kind:str}/{file_name:str}",
    # Синхронизация каталога. Открыты для общей охраны и аутентифицируются
    # сами, токеном SCIM: у провайдера учётных записей нет ни человека, ни
    # сессии, ни cookie, и токен доступа ему выдать нельзя.
    #
    # Собственная охрана этих маршрутов строже общей. Она сверяет выключатель
    # синхронизации рабочего пространства — выключенная означает отказ даже
    # при действительном токене, — принадлежность токена пространству и то,
    # что токен не отозван и не выключен.
    #
    # Два маршрута обнаружения (`ServiceProviderConfig`, `ResourceTypes`)
    # отдают только описание возможностей сервера. Данных рабочего
    # пространства в них нет, и провайдер читает их до того, как получит
    # токен: закрыв их, мы сделали бы настройку интеграции невозможной.
    "/api/scim/v2/ServiceProviderConfig",
    "/api/scim/v2/ResourceTypes",
    "/api/scim/v2/Users",
    "/api/scim/v2/Users/{user_id:str}",
    "/api/scim/v2/Groups",
    "/api/scim/v2/Groups/{group_id:str}",
    # Экран входа рисуется до того, как появилась сессия, а нарисовать его без
    # имени и вида провайдеров нечем. Отдаются только те поля, которые всё
    # равно видны на странице входа: настройки провайдера — секрет клиента,
    # пароль служебной учётной записи каталога — сюда не попадают.
    "/api/workspace/public",
    # Вход через провайдера. Публичны по той же причине, что и парольный вход:
    # человек приходит сюда до того, как у него появилась сессия, а на
    # обратный вызов его присылает провайдер переходом браузера.
    #
    # Общей охраны здесь нет, но защита есть у каждого протокола своя, и она
    # строже общей. OIDC: сверка `state`, PKCE и одноразового значения плюс
    # проверка подписи токена по набору ключей провайдера. SAML: проверка
    # подписи утверждения, чтение только подписанного поддерева и подписанный
    # нами `RelayState`. Каталог: проверка пароля в самом каталоге.
    #
    # На всех трёх стоит счётчик частоты по адресу, на входе через каталог —
    # второй, по паре провайдера и имени.
    "/api/sso/oidc/{provider_id:uuid}/login",
    "/api/sso/oidc/{provider_id:uuid}/callback",
    "/api/sso/saml/{provider_id:uuid}/login",
    "/api/sso/saml/{provider_id:uuid}/callback",
    "/api/sso/ldap/{provider_id:uuid}/login",
    # Канал событий. Открыт для общей охраны и аутентифицируется сам: у
    # рукопожатия нет ни разобранного токена, ни сессии базы, и охрана маршрута
    # ему ничего дать не может.
    #
    # Собственная проверка строже общей. Тот же вид токена, обязательный
    # идентификатор сессии, её отзыв и срок, отключённость и удаление человека
    # — и сверх того сверка происхождения, которой у обычного запроса нет.
    # Комнаты назначает только сервер: попросить их клиент не может.
    "/socket.io",
    # Внутренние маршруты совместного редактирования. Открыты для общей охраны
    # и аутентифицируются сами, общим секретом: сюда ходит не человек, а сосед
    # на Node, у которого нет ни куки, ни сессии. Пропускать его общей охраной
    # значило бы завести ему учётную запись с правом писать в любую страницу.
    #
    # Собственная проверка иная, но не слабее. Секрет задаётся переменной
    # окружения и сверяется постоянным по времени сравнением; пустое значение
    # выключает маршруты вовсе, то есть развёртывание без соседа их не
    # открывает. Право человека на страницу проверяется отдельно и на каждом
    # вызове: секрет подтверждает, что спрашивает сосед, а не что спрашивающему
    # что-то можно.
    "/api/internal/collab/authorize",
    "/api/internal/collab/document",
    "/api/internal/collab/store",
    "/api/internal/collab/rights",
}


def _settings() -> Settings:
    return Settings(
        database_url="postgresql://tessera:x@127.0.0.1:5432/tessera",
        redis_url="redis://127.0.0.1:6379",
        app_secret="x" * 32,
        app_url="http://localhost:3000",
        port=3000,
        host="0.0.0.0",
        debug=False,
        trust_proxy_hops=1,
    )


def _public_paths(app: Litestar) -> set[str]:
    """Все публичные маршруты, каким бы способом они ни были объявлены.

    Обходятся оба вида: обычные обработчики держат их списком в
    `route_handlers`, смонтированное ASGI-приложение — одним в `route_handler`.
    Проверка, знающая только про список, пропустила бы монтирование целиком, то
    есть самый крупный вид публичной поверхности молча не попадал бы в опись.
    """
    found: set[str] = set()
    for route in app.routes:
        handlers = list(getattr(route, "route_handlers", []))
        single = getattr(route, "route_handler", None)
        if single is not None:
            handlers.append(single)
        for handler in handlers:
            if handler.opt.get(PUBLIC):
                found.add(route.path)
    return found


def test_route_scan_is_not_empty() -> None:
    """Сначала проверяется сам обход.

    Пустая выборка дала бы зелёную проверку на пустом множестве: она
    подтверждала бы, что лишних публичных маршрутов нет, потому что не нашла
    ни одного вообще.
    """
    app = create_app(_settings())
    assert len(app.routes) > 5


def test_only_expected_routes_are_public() -> None:
    app = create_app(_settings())
    assert _public_paths(app) == EXPECTED_PUBLIC
