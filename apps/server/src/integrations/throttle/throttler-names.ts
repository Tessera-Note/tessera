export const AUTH_THROTTLER = 'auth';
export const AI_CHAT_THROTTLER = 'ai-chat';
export const EXPORT_THROTTLER = 'export';
/**
 * Отдельный счетчик для входа через каталог.
 *
 * Общий лимит по адресу за корпоративным NAT делится всеми сотрудниками,
 * а перебор пароля через нас накручивает счетчик неудач в каталоге и
 * блокирует настоящую учетную запись. Поэтому нужен второй счетчик,
 * привязанный к провайдеру и имени пользователя.
 */
export const LDAP_LOGIN_THROTTLER = 'ldap-login';

/**
 * Отдельный счетчик для отрисовки PDF.
 *
 * Маршрут отрисовки публичный и зовет его безголовый браузер Gotenberg из
 * своего контейнера, то есть весь экземпляр приходит с одного адреса. Общий
 * счетчик выгрузки, десять запросов в минуту, тут означал бы десять отрисовок
 * в минуту на всех.
 *
 * Отказ на этом маршруте к тому же не чистый: страница отрисовки помечает себя
 * готовой и при ошибке, поэтому в PDF попадает текст отказа, а задача остается
 * успешной. Порог здесь нужен как потолок против перебора токена отрисовки, а
 * не как рабочее ограничение, поэтому он заведомо выше любой настоящей
 * нагрузки.
 */
export const PDF_RENDER_THROTTLER = 'pdf-render';
