/**
 * `$app/state` для проверок разметки.
 *
 * SvelteKit подставляет этот модуль сам, а проверки идут мимо него. Здесь
 * оставлено то, чем пользуются экраны: адрес страницы. Проверка ставит его до
 * отрисовки — так же, как это делает переход по ссылке.
 */
export const page: { url: URL; params: Record<string, string> } = {
  url: new URL('http://localhost/'),
  params: {}
};
