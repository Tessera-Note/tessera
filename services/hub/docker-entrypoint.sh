#!/usr/bin/env bash
# Применяет миграции и запускает сервис.
# Миграции идут до старта: схема обязана существовать раньше, чем приложение
# начнет наполнять базу стартовым содержимым.
set -euo pipefail

alembic upgrade head

exec "$@"
