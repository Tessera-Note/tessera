**/node_modules
**/.git
**/dist
/data
**/.env*
**/.nx
# The build of the screens and the SvelteKit working directory. The screens
# image is built with the whole repository as context, and without these lines a
# local build travels into the context only to be overwritten right away by the
# build inside the image.
/apps/web/build
/apps/web/.svelte-kit
